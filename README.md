# arch_oomox

Role to generate oomox themes.
The icon theme plugin script is found dynamically by getting the package listing from pacman and piping to the custom filter, `find_binary`.

This role is not meant to be general purpose - it is used for bootstrapping my personal machines.
